---
services:
  pihole:
    # In CI, remove published ports to avoid host exposure.
    # Services communicate only via internal Docker networks.
    ports: !reset []

  traefik:
    # In CI, bind to all interfaces instead of SERVER_LOCAL_IP (which doesn't exist on the runner).
    ports: !reset
      - "80:80"
      - "443:443"
